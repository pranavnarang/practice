// Given a list of words and two words word1 and word2, return the shortest distance between these two words in the list.

// For example,
// Assume that words = ["practice", "makes", "perfect", "coding", "makes"].

// Given word1 = “coding”, word2 = “practice”, return 3.
// Given word1 = "makes", word2 = "coding", return 1.

var returnShortestDistance = (words, wordOne, wordTwo) =>{
    // create an array of word 1 containing index of each letter

    let wordOneIndex = [];
    let latestIndex = 
    while(words.indexOf(wordOne) !== -1){

    }
    
    return 1;
}

module.exports = returnShortestDistance;


["practice", "makes", "perfect", "coding", "makes", "practice"]

indexOf practice = 0, 5
indexOf coding = 4,9,11